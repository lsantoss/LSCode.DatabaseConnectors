namespace LSCode.DatabaseConnectors.Enums
{
    /// <summary>Contains list of supported non-relational databases.</summary>
    public enum EDatabaseNonRelational
    {
        /// <value>Used when connecting to MongoDB databases.</value>
        MongoDB = 1,

        /// <value>Used when connecting to Redis databases.</value>
        Redis = 2
    }
}